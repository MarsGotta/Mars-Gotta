import React from 'react';
import PropTypes from 'prop-types';
import styles from './MarsSlide.module.css'

const MarsSlide = ({
}) => {
  return (<div className={styles['mars-button']}>Hola Soy un boton</div>);
};

MarsSlide.defaultProps = {
};

MarsSlide.propTypes = {
};

export default MarsSlide;
